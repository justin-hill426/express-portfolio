extends layout.pug

block content

  article.portfolio-intro

    h1 Let's make the web great together!

    p This portfolio is about all the amazing things that I'm creating in my current career.


  article.portfolio-project-thumbs
    .projects-box.box.grid
      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`${project.image_urls}`, alt=`${project.project_name} Thumbnail`)
            h5=`${project.project_name}`

